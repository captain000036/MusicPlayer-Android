[app]

# 應用程式名稱
title = MusicPlayer

# 套件名稱（只能小寫英數）
package.name = musicplayer

# 套件網域
package.domain = org.test

# 原始碼目錄
source.dir = .

# 必須包含字型，否則中文會顯示異常
source.include_exts = py,png,jpg,kv,atlas,otf,ttf,json

# 版本號
version = 1.0

# =========================================================
# 🔑 關鍵：與你目前 main.py「完全相容」的依賴
# =========================================================
requirements = python3,kivy,yt-dlp,ffmpeg,openssl,certifi

# ⚠️ 說明：
# - yt-dlp：YouTube 搜尋 / 下載
# - ffmpeg：只供 yt-dlp 音訊轉封裝（不會跟 Android 內建衝突）
# - openssl + certifi：避免 HTTPS / 圖片 / 搜尋時隨機閃退
# - ❌ 不使用 android / pyjnius / ffpyplayer（避免播放器衝突）

# =========================================================
# 顯示與方向
# =========================================================
orientation = portrait
fullscreen = 0

# =========================================================
# ✅ 中文輸入法修正（非常重要）
# =========================================================
android.window_softinput_mode = resize

# =========================================================
# Android 權限
# =========================================================
android.permissions = INTERNET,ACCESS_NETWORK_STATE,READ_EXTERNAL_STORAGE,WRITE_EXTERNAL_STORAGE,WAKE_LOCK

# =========================================================
# Android API 設定（符合 Play 商店）
# =========================================================
android.api = 33
android.minapi = 23

# 主流 64 位元架構
android.archs = arm64-v8a

# =========================================================
# 啟動設定
# =========================================================
android.entrypoint = org.kivy.android.PythonActivity
android.accept_sdk_license = True

# =========================================================
# python-for-android 分支
# =========================================================
p4a.branch = master

# =========================================================
# 圖示與啟動畫面（若沒有檔案請保持註解）
# =========================================================
# icon.filename = %(source.dir)s/icon.png
# presplash.filename = %(source.dir)s/presplash.png


[buildozer]

# 顯示詳細編譯 log（除錯必開）
log_level = 2

warn_on_root = 1
