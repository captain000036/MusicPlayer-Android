name: Build Android APK
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. 下載程式碼
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. 釋放磁碟空間 (Android 打包必備，沒有這步會失敗)
      - name: Free Disk Space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      # 3. 使用 Buildozer Action 打包
      # 【關鍵修正】將 @v1 改為 @master，解決 Docker 映像檔遺失的問題
      - name: Build with Buildozer
        uses: ArtemSBulgakov/buildozer-action@master
        id: buildozer
        with:
          command: buildozer android debug
          buildozer_version: master

      # 4. 上傳打包好的 APK
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: MusicPlayer-APK
          path: bin/*.apk
          retention-days: 3
          compression-level: 0
